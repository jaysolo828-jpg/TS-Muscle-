<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Health Connect declares minSdk 26 in its own manifest. We override
         that here because our HealthConnectSyncActivity gates every HC call
         behind getSdkStatus() — devices below API 26 hit SDK_UNAVAILABLE
         and finish silently, so there is no runtime risk. -->
    <uses-sdk tools:overrideLibrary="androidx.health.connect.client" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- FOREGROUND_SERVICE_HEALTH required on Android 14+ when foregroundServiceType="health" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH"
        android:minSdkVersion="34" />
    <!-- ACTIVITY_RECOGNITION required on Android 10+ to read TYPE_STEP_COUNTER -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Step counter hardware is optional — app works without it (timer still runs) -->
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />

    <!-- Health Connect: exercise sessions, distance, calories, and steps -->
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <!-- Health Connect app package (required on Android 9–13) -->
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:label="@string/appName"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Launcher">

            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="@string/launchUrl" />

            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/colorPrimary" />

            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:resource="@color/backgroundColor" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/navigationBarColor" />

            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR_DARK"
                android:resource="@color/navigationBarColor" />

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="app.therapyandsneakers.org"
                    android:scheme="https" />
            </intent-filter>
        </activity>

        <activity android:name="com.google.androidbrowserhelper.trusted.FocusActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name="com.google.androidbrowserhelper.trusted.NotificationPermissionRequestActivity" />

        <!-- Small bridge activity invoked from the web page via a custom-scheme
             intent URL. Its sole job is to call ActivityCompat.requestPermissions
             for POST_NOTIFICATIONS so the real Android OS dialog is shown even
             when Chrome's cached per-origin permission is 'granted' (the split
             state that cannot be unstuck from JavaScript). -->
        <activity
            android:name=".EnableNotificationsActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ts-muscle-enable-notifs" />
            </intent-filter>
        </activity>

        <!-- Health Connect sync bridge. Triggered from the web page via a
             custom-scheme intent URL. Reads exercise sessions from Health
             Connect and posts them directly to Supabase using the user's
             JWT, then finishes — returning the user to the Chrome tab. -->
        <activity
            android:name=".HealthConnectSyncActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ts-muscle-hc-sync" />
            </intent-filter>
            <!-- Required by Play Store for apps using Health Connect permissions.
                 This intent lets Health Connect show users which data your app
                 accesses from the Health Connect permissions screen. -->
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Step counter bridge. Launched from the web page via:
             intent://start#Intent;scheme=ts-muscle-step-start;...;end
             Requests ACTIVITY_RECOGNITION, then starts StepCounterService. -->
        <activity
            android:name=".StepPermissionActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ts-muscle-step-start" />
            </intent-filter>
        </activity>

        <!-- Foreground service that reads TYPE_STEP_COUNTER and serves step
             count over http://localhost:8765/steps for the web page to poll. -->
        <service
            android:name=".StepCounterService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <service
            android:name=".TSFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <service
            android:name=".PaymentDelegationService"
            android:exported="true"
            android:enabled="true">

            <meta-data
                android:name="android.support.customtabs.trusted.SMALL_ICON"
                android:resource="@drawable/ic_ts_notification" />

            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- Play Billing payment handler. Chrome's PaymentRequest discovers
             apps that can fulfil https://play.google.com/billing by searching
             for activities with this intent-filter and meta-data. Without
             these declarations, PaymentRequest rejects the method as
             "not supported" before .show() can open the Play purchase sheet.
             The classes ship in com.google.androidbrowserhelper:billing. -->
        <activity
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:configChanges="keyboardHidden|keyboard|orientation|screenLayout|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="org.chromium.intent.action.PAY" />
            </intent-filter>
            <meta-data
                android:name="org.chromium.default_payment_method_name"
                android:value="https://play.google.com/billing" />
        </activity>

        <service
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentService"
            android:exported="true">
            <intent-filter>
                <action android:name="org.chromium.intent.action.IS_READY_TO_PAY" />
            </intent-filter>
        </service>

        <!-- Native Play Billing bridge. Replaces the broken Chrome-mediated
             TWA Play Billing path (clientAppUnavailable). Launched from JS
             via intent://buy#Intent;scheme=ts-muscle-subscribe;...;S.sku=...;end.
             Calls Play Billing Library directly, returns to the TWA with
             ?subscribe=success&subscribe_token=... in the URL. -->
        <activity
            android:name=".SubscribeActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ts-muscle-subscribe" />
            </intent-filter>
        </activity>

    </application>
</manifest>
